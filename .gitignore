# Native Host 备份文件
*.bak

# 临时文件
*.tmp
*.log

# IDE 配置
.vscode/
.idea/
.trae/

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# Node.js (如有)
node_modules/

# 打包文件
*.zip

# 文档
.trae/

# TRAE 工具产物（不应提交到仓库）
agent-browser-*.exe

# AOS 状态文件（不应提交到仓库）
AGENT_STATUS.md
STATUS.md
AGENTS.md
reference_links.md

# 扩展签名私钥（用于固定扩展 ID，泄露后他人可冒充发布更新）
credentials/
*.pem
*.key

# 开发副本（本地备份，不入 git，发行版 source code 不包含）
# 开发时直接改 extension/，versions/ 仅作本地历史备份
versions/

# 本地图标源文件（仅本地用于 resize_icons.ps1 生成扩展图标）
icons_preview/

# 本地开发工具脚本（仅本地使用，不提交到 GitHub）
tools/